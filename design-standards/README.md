# Distilled Design Standards

This directory stores the stable distilled output of creator research.

Raw covers, source captures, full research notes, and manifests belong in the
managed research workspace outside Git:

```text
J:\PigeonYang\cover-style-distiller\research-runs
```

Each creator directory should contain the latest canonical `design-standard.md`
used to build or audit the matching child skill.

